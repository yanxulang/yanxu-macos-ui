引「标准:原生」为 原生；

引「应用模型.yx」为 模型；

公 定 原生宿主名：文 为 「yanxu-macos-ui」；

令 宿主模块：任意 为 空；

法 取宿主（）：原生模块 则
    若 （宿主模块 是 原生模块） 则
        归 宿主模块；
    终
    定 模块：原生模块 为 原生.加载（原生宿主名）；
    置 宿主模块 为 模块；
    归 模块；
终

公 法 运行（应用：麦金塔应用）：空 则
    定 处理器：法 为 应用.事件处理器；
    法 转发事件（名称：文，载荷：典）：空 则
        处理器（模型.事件（名称，载荷））；
        归 空；
    终
    原生.调用（取宿主（），「run」，【应用.JSON（），转发事件】）；
    归 空；
终

公 法 更新（应用：麦金塔应用）：空 则
    原生.调用（取宿主（），「update」，【应用.JSON（）】）；
    归 空；
终
