引「标准:测试」为 测试；
引「../src/言序麦金塔界面.yx」为 界面；

定 标题变化 为 界面.动作（「title.changed」）；
定 增加动作 为 界面.动作（「counter.increment」）；
定 输入 为 界面.文本框（「title」，「窗口标题」，「言序」）.当文本变化（标题变化）；
定 根 为 界面.垂直（【
    界面.标题（「可交互应用」）,
    输入,
    界面.按钮（「增加」，增加动作）
】）.设间距（12）；
根.内边距（20）；
定 应用 为 界面.应用（「测试」）.添窗口（界面.窗口（「主窗口」，根））；
定 描述 为 应用.描述（）；

测试.相等（界面.版本，「0.7.1」）；
测试.相等（描述【「schema」】，「dev.yanxu.mac-ui.v2」）；
测试.相等（描述【「windows」】【0】【「root」】【「children」】【1】【「event」】，「title.changed」）；
测试.相等（描述【「windows」】【0】【「root」】【「children」】【2】【「event」】，「counter.increment」）；
测试.相等（描述【「windows」】【0】【「root」】【「style」】【「spacing」】，12）；
测试.相等（描述【「windows」】【0】【「root」】【「style」】【「padding」】，20）；

定 兼容视图 为 界面.视图（「Text」，{「text」：「兼容」}, 【】）.设（「help」，「低层扩展入口」）；
测试.相等（兼容视图.描述（）【「text」】，「兼容」）；

法 处理（所事件）：空 则 归 空；终
应用.当事件（处理）；
测试.断言（应用.事件处理器 是 法，「应用须保存言序事件处理器」）；

定 标题状态 为 界面.文字状态（「document.title」，「未命名」）；
定 显示状态 为 界面.布尔状态（「inspector.visible」，假）；
定 选择状态 为 界面.选择状态（「tasks.selection」，【】）；
定 状态输入 为 界面.绑定文本框（「bound-title」，「标题」，标题状态.绑定（））；
定 状态应用 为 界面.应用（「状态测试」）
    .添状态（标题状态）
    .添状态（显示状态）
    .添状态（选择状态）
    .添窗口（界面.窗口（「状态」，界面.垂直（【
        状态输入,
        界面.绑定开关（「bound-visible」，「显示」，显示状态.绑定（））,
        界面.选择列表（「task-list」，【界面.选项（「任务一」，「task-1」）】，选择状态.绑定（））
    】）））；
定 状态描述 为 状态应用.描述（）；
测试.相等（状态描述【「state」】【0】【「id」】，「document.title」）；
测试.相等（状态描述【「windows」】【0】【「root」】【「children」】【0】【「binding」】，「document.title」）；

令 已先回写：理 为 假；
法 处理状态事件（所事件）：空 则
    置 已先回写 为 标题状态.取值（） 等于 「新标题」；
    归 空；
终
状态应用.当事件（处理状态事件）；
状态应用.接收原生事件（「binding.changed」，{「binding」：「document.title」，「value」：「新标题」}）；
测试.断言（已先回写，「绑定值必须在用户处理器执行前写回言序状态」）；
测试.相等（标题状态.取值（），「新标题」）；

定 控件面板 为 界面.垂直（【
    界面.选择器（「priority」，「优先级」，【界面.选项（「高」，「high」）】，标题状态.绑定（）），
    界面.搜索框（「search」，「搜索」，标题状态.绑定（）），
    界面.标签（「完成」，「checkmark.circle」），
    界面.链接（「文档」，「https://github.com/yanxulang/yanxu-macos-ui」）
】）；
测试.相等（控件面板.描述（）【「children」】【0】【「kind」】，「Picker」）；
测试.相等（控件面板.描述（）【「children」】【1】【「kind」】，「SearchField」）；

定 菜单栏内容 为 界面.垂直（【
    界面.标签（「菜单栏」，「star.fill」），
    界面.绑定开关（「menu-toggle」，「显示」，显示状态.绑定（））
】）；
定 菜单栏应用 为 界面.应用（「菜单栏测试」）
    .添状态（显示状态）
    .添菜单栏项目（界面.菜单栏项目（「status-main」，「star.fill」，「菜单栏测试」，菜单栏内容）.尺寸（280，240））；
定 菜单栏描述 为 菜单栏应用.描述（）；
测试.相等（长度（菜单栏描述【「windows」】），0）；
测试.相等（菜单栏描述【「menuBarItems」】【0】【「id」】，「status-main」）；
测试.相等（菜单栏描述【「menuBarItems」】【0】【「content」】【「children」】【1】【「binding」】，「inspector.visible」）；
测试.断言（长度（菜单栏应用.JSON（）） 大于 0，「纯菜单栏应用必须可以序列化」）；

定 文档内容 为 界面.文字状态（「document.content」，「」）；
定 文档路径 为 界面.文字状态（「document.path」，「」）；
定 表格选择 为 界面.选择状态（「table.selection」，【】）；
定 表格视图 为 界面.表格（「release-table」，【
    界面.表格列（「版本」，「version」）
】， 【
    界面.表格行（「v0.6」）.设值（「version」，「0.6.0」）
】，表格选择.绑定（））.设排序键（「version」）；
定 文档场景 为 界面.文档场景（「text-document」，「文档」，【「public.plain-text」】，界面.绑定多行文本（「document-editor」，文档内容.绑定（）））
    .绑定内容（文档内容.绑定（））
    .绑定路径（文档路径.绑定（））；
定 完整应用 为 界面.应用（「完整测试」）
    .添状态（文档内容）
    .添状态（文档路径）
    .添状态（表格选择）
    .添场景（界面.窗口场景（「main」，「主窗口」，表格视图））
    .添场景（界面.设置场景（界面.文本（「设置」）））
    .添场景（文档场景）；
定 完整描述 为 完整应用.描述（）；
测试.相等（完整描述【「windows」】【0】【「id」】，「main」）；
测试.相等（完整描述【「windows」】【0】【「root」】【「kind」】，「Table」）；
测试.相等（完整描述【「documents」】【0】【「contentBinding」】，「document.content」）；
测试.相等（完整描述【「settings」】【「kind」】，「Text」）；

定 打开请求 为 界面.打开请求（「open-text」，【「public.plain-text」】，假）.作为导入（）；
测试.相等（打开请求.描述（）【「type」】，「file.import」）；
测试.相等（打开请求.描述（）【「id」】，「open-text」）；
定 网络请求 为 界面.HTTP请求（「load-data」，「GET」，「https://example.com/data」，{「Accept」：「application/json」}，「」，15）；
测试.相等（网络请求.描述（）【「type」】，「http.request」）；
测试.相等（网络请求.描述（）【「headers」】【「Accept」】，「application/json」）；
定 存储请求 为 界面.安全存储请求（「save-token」，「set」，「dev.yanxu.test」，「token」，「secret」）；
测试.相等（存储请求.描述（）【「type」】，「secure.set」）；
定 停止定时 为 界面.定时器请求（「stop-refresh」，假，0）.停止目标（「refresh-timer」）；
测试.相等（停止定时.描述（）【「timerID」】，「refresh-timer」）；
定 偏移时钟 为 界面.时钟请求（「minute-ago」，-60）；
测试.相等（偏移时钟.描述（）【「offset」】，-60）；
