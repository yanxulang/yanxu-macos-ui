引「../src/言序麦金塔界面.yx」为 界面；
引「标准:测试」为 测试；
引「标准:文字」为 文字；

法 含片（原文：文，片：文）：理 则
    归 长度（文字.分割（原文，片））大于 1；
终

定 根 为 界面.垂直（【
    界面.标题（「言序原生界面」）,
    界面.文本框（「projectName」，「项目名」，「yanxu」）,
    界面.水平（【
        界面.按钮（「构建」，「build.requested」）,
        界面.开关（「preview」，「预览」，真）
    】）
】）.设样式（「padding」，16）.无障碍标签（「主界面」）；

定 窗 为 界面.窗口（「言序」，根）.尺寸（960，640）.最小尺寸（520，360）.添工具项（「run」，「运行」，「run.requested」）；
定 应用 为 界面.应用（「言序」）.设强调色（「teal」）.添窗口（窗）.添菜单项（「关于言序」，「app.about」）；
定 描述 为 应用.描述（）；
测试.相等（描述【「schema」】，「dev.yanxu.mac-ui.v1」）；
测试.相等（描述【「windows」】【0】【「title」】，「言序」）；
测试.相等（描述【「windows」】【0】【「root」】【「kind」】，「VStack」）；
测试.相等（描述【「windows」】【0】【「toolbar」】【0】【「event」】，「run.requested」）；

定 JSON文：文 为 应用.JSON（）；
测试.断言（含片（JSON文，「dev.yanxu.mac-ui.v1」），「应用 JSON 须包含 schema」）；
测试.断言（含片（JSON文，「build.requested」），「应用 JSON 须包含按钮事件」）；
测试.相等（界面.默认运行器，「yanxu-macos-ui-runner」）；

令 已拒绝空应用：理 为 假；
试 则
    界面.应用（「空」）.JSON（）；
救 所误 则
    置 已拒绝空应用 为 真；
终
测试.断言（已拒绝空应用，「没有窗口的应用不能启动」）；

令 已拒绝坏事件：理 为 假；
试 则
    界面.按钮（「坏」，「1.bad」）；
救 所误 则
    置 已拒绝坏事件 为 真；
终
测试.断言（已拒绝坏事件，「事件名必须是稳定标识」）；

令 已拒绝负尺寸：理 为 假；
试 则
    界面.窗口（「坏」，界面.文本（「x」））.尺寸（减 1，200）；
救 所误 则
    置 已拒绝负尺寸 为 真；
终
测试.断言（已拒绝负尺寸，「窗口尺寸不得为负」）；
