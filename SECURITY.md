# 安全策略

安全问题请通过 GitHub Security Advisory 私下报告，不要在公开 issue 中附带可利用载荷。

`yanxu-macos-ui` 的言序层只生成界面描述，不执行任意系统命令。当前版本通过言序 1.1.7 原生 ABI v2 装载麦金塔宿主制品；顶层应用必须显式声明 `原生扩展 = true`，运行时会校验目标、大小与 SHA-256。应用事件名和视图标识会做保守校验，传给原生层的业务载荷仍应由应用自行验证。
